procs:
  plugin:
    shell: "cd plugin && npm i && npm run dev"
  server:
    shell: "cd server && bun i && bun run dev"
  db:
    shell: "./db_setup.sh"
  pluginmove:
    shell: "./plugin/movestuff.py"
  tunnel:
    shell: "./server/tunnel.py"

